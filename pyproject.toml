[project]
name = "starranja_backend"
version = "0.1.0"
description = "Backend for the Starranja app"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.119.0",
    "httpx>=0.28.1",
    "pytest>=8.4.2",
]
